 name: Example Pipeline

 on:
    push:
        branches:
            - test-actions
 jobs:
    build: 
        runs-on: ubuntu-latest
        
        steps:
         - name: Checkout Repository
           uses: actions/checkout@v2
    
